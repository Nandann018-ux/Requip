# Database — matches docker-compose.yml credentials
DATABASE_URL="mysql://appuser:apppassword@localhost:3306/user_management"

# App
PORT=3001
NODE_ENV=development

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
